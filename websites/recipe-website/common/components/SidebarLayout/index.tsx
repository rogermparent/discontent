// Promoted to @discontent/component-library so portfolio's settings area can
// use the same contained, masthead-aligned sidebar (portfolio rebuild PR C).
// Re-exported here so recipe's existing import paths keep working.
export {
  SidebarLayout,
  type SidebarLayoutProps,
} from "@discontent/component-library/components/SidebarLayout";
