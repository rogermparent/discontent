// Promoted to @discontent/component-library so every content-engine site shares
// one theming layer (portfolio rebuild PR 01b). Re-exported here so recipe's
// existing import paths keep working.
export {
  ThemeVarsProvider,
  useThemeVars,
} from "@discontent/component-library/components/theming/ThemeVarsProvider";
